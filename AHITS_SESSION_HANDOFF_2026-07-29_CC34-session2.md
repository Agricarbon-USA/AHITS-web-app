# AHITS — Session Handoff · 2026-07-29 · CC-34 (maintenance speaks) — SESSION 2 of 2 (PR-3)

> Session split (per RIDER C): session 1 = PR-1 + PR-2 (MERGED); **session 2 = PR-3 (schedules — the value center)** (this doc).
> **PR-3 (#229) is OPEN + full CI green.** Nothing merged this session. Decisions referenced by `Dn` — see `DECISIONS.md` (D29 was recorded in session 1; **do not re-append**). D24 gained one note line this session.

## What shipped (as an OPEN PR — not merged)

**PR-3 (#229) — "schedules become real" (D29-1).** Base `development`. Branched off current `development` — PR-1 #226 + PR-2 #227 are already merged, so 1b's `rigId` + the `returnDestination*` columns are live; PR-3 is a **standalone branch, not a stack**. **No migration** (reuses those columns → previewable; migration-safety had nothing to scan). **CI fully green:** `Backward-compatible migrations` pass, `Live-branch DROP guard` pass, `verify / Lint, type-check & build` pass, **`verify / Tests` pass** (ran the new node DB suite `tests/cc34-schedules-and-alerts.test.ts` — which could not run locally; the node DB suite is CI-only here).

- **3a — Add-scheduled-task dialog** (`admin/maintenance/page.tsx`, `api/maintenance/route.ts`). The first UI caller of `POST /api/maintenance`, so **D24's named Wintex/Giddings service rows are finally enterable** (the template bridge's other half; checklist items via the admin editor were already the day-one path). `SearchableSelect` subject (vehicle/item), interval **MILEAGE / DAYS / MONTHS only — PER_DEPLOYMENT NOT offered** (D29-4; the zod enum + `schema.prisma:126` keep it dormant), native `TextField type="date"` (x-date-pickers gone since CC-33). Server defaults an omitted `nextDue` (DAYS/MONTHS → `nextDueFromInterval`) / `nextOdometer` (MILEAGE → `vehicle.odometer + interval`) so a schedulable task is never born unschedulable.
- **3b — stale-damage cron nag** (`cron/dispatch/route.ts`, `notifications.ts`). Damage tasks untouched > 7d raise a `MAINTENANCE_OVERDUE` with a `staleDamageDays` marker (RIDER C #9 — reuse the type, no enum change; activeKey can't collide). **Bounded `orderBy updatedAt asc, take 5`** per run. Copy: *"<task> — no updates in N days. Worth a look."* Self-resolves on complete; any edit re-arms the 7-day clock.
- **3c — two-click shop chips + DEPLOYMENT return + stranded-unit fix** (`admin/maintenance/page.tsx` + `api/maintenance/[id]/complete/route.ts`). Drawer chips *Delivered to shop today* / *Repair started* (PATCH via the existing helper). Close dialog gains a **DEPLOYMENT** destination (active rig, the API already validated it). **RIDER C #10 (server, NOT droppable):** on complete, a unit still in an **open** kit item on an **active** rig restores to **CHECKED_OUT**, not AVAILABLE — applied to **both** restore paths (direct + UR-029 fallback). Post-PR-2 this is a live double-issue hazard, so it shipped even though the 3c *chips* were droppable.
- **3d — operator "In repair" visibility** (`api/maintenance/route.ts`, `DeploymentCards.tsx`, `my-deployment/page.tsx`). `?rigId=` where-arm (one-liner off 1b's column; already operator-readable, no auth change). My-Deployment renders a read-only caption **"In repair — <status>"** — human label via `lib/status.ts`, **no chip+caption stacking** (RIDER C #12). Repair maps memoized so the CC-12 memo boundary holds. `operator-today.ts` + `sw.ts` deliberately untouched.
- **3e — WORK_ORDER-completed real alert** (`status-links.ts`, `notifications.ts`). COMPLETED branch now `resolveActiveAlert` → `createAlert('DAMAGE_REPORTED', …, { phase:'READY_TO_FINALIZE', shop })` **post-commit** (resolve-then-create so `notifiedAt` is null and the dispatcher bells + emails it once, deduped). RECEIVED/IN_PROGRESS/INVOICED keep bell-only `notifyAdmins`. *Behavioral delta:* the COMPLETED notification now arrives on the next cron dispatch (≤~1 min) rather than synchronously, in exchange for dedup + email + Active-Alerts presence.

## ⛔ PRE-MERGE GATE (Max) — the one blocking item before the merge go
Run the **stale-damage count SELECT read-only against staging** (the exact query is in the PR #229 body): `isDamageReport = true AND status <> 'COMPLETED' AND deletedAt IS NULL AND updatedAt < now() − 7 days`. Paste the count/rows into the PR. **If > 15 rows, triage the list BEFORE deploy** — the first fixed cron run will nag each (bounded 5/run). I could not run it from the sandbox (staging direct URL is IPv6-only + needs gcloud secret access).

## Owed before merge (Max)
- The pre-merge SELECT above, then **evening staging smoke**, then merge. Previewable after (no migration). Smoke set (PR body): create a 90-day Wintex schedule on a carrier in < 1 min → Upcoming with a real due date; a back-dated one → Overdue + bell on next cron; close a unit repair back to an active deployment in ≤ 3 clicks; after an "Out of service" report, My Deployment shows "In repair — In Progress" and clears on close.
- **On merge:** mark CC-34 **fully landed** in STATUS §1 + the ledger (`AHITS_CLAUDE_CODE_INSTRUCTIONS.md`) — this session left both as "PR-3 open + green" per the packet's "fully landed only after PR-3 merges" rule.

## Step-7 verify anchors (re-verified green this session)
`Add scheduled task` + `nextDueFromInterval` (3a) · `staleDamageDays` in cron + notifications (3b) · close-dialog `DEPLOYMENT` + the stranded-unit `CHECKED_OUT` branch (3c) · `rigId` filter + `In repair` caption (3d) · `READY_TO_FINALIZE` in status-links + notifications (3e) · `PER_DEPLOYMENT` still in `schema.prisma:126` AND absent from the new dialog's interval options (only a comment references it).

## Notes for the next session / worktree hygiene
- Code lives in the isolated worktree `~/Downloads/ahits-cc34-pr3-worktree` on branch `feature/20260729/Agricarbon-USA-cc34-pr3-schedules` (a `node_modules` symlink there is intentional and untracked). Explicit-path staging was used throughout (never `git add -A`). The worktree can be removed after #229 merges (`git worktree remove`).
- D24 is now fully bridged: checklist items (admin editor, day-one) **and** the new scheduled-task dialog (PR-3a) both make the mounted-unit schedules enterable.
