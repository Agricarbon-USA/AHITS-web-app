# AHITS — STATUS  ·  the one always-current doc — read me first

> ## 🚀 LAUNCH BOX — pilot start POSTPONED → ROLLING START (D17, 2026-07-28)
> - **The Mon 07-27 start did not happen** — operational (equipment/weather/project) delays, not app readiness. Operators now onboard **rolling**, as CUL005 spins up. The charter's fortnight window starts at first-operator-live.
> - **iOS A6-Lite: ✅ PASSED** (ran ~07-24, retro-recorded 2026-07-28 — see the A6 checklist banner + D17). No device gate remains for the rolling start — **the gate is now the pilot-floor build queue**.
> - **CC-29 (offline trust floor) — ✅ MERGED to `development`/staging (PRs #202, #203, #205), 2026-07-28.** All three chunks landed: queue engine (photo-wedge/lie-fi-timeout/online-401-park/concurrent-flush/dead-localphoto), daily-check date semantics (D26), sliding session renewal (D27). CI green incl. the node DB suite + the new `tests/offline` flush-lifecycle harness (the review's #1 missing test). **REMAINING GATE before first operator: Max's on-device staging smoke** — the airplane-mode → reconnect cycle, the photo-wedge/Discard, the overnight-queue replay, and the session-renewal boundary (these need a physical phone; not scriptable). **CC-32 (friction & flow) ✅ ALL MERGED (#209/#210/#211/#212).** **CC-30 ✅ merged.** **CC-31 (accuracy + /admin/pilot dashboard) ✅ FULLY MERGED (#213/#214/#215 + PR-1), preview-smoked.** **CC-16S ✅ MERGED (#220/#221).** **CC-33 (simplify & unify) ✅ MERGED (#223 dead-code + forward→operator removal (D21) · #224 one unified Transfer entry (D22)), 2026-07-29 — D21/D22 EXECUTED; two-phone Transfer smoke owed (Max).** **CC-34 (maintenance-speaks) — ✅ FULLY LANDED (PR-1 #226 + PR-2 #227 + PR-3 #229 all MERGED, staging deploy green). Residual: Max's staging smoke + stale-damage SELECT.** Packets: `AHITS_CC29-31_PILOT_FLOOR_PACKETS.md` + riders in `AHITS_PACKET_ERRATA_2026-07-29.md`. Findings they close: `AHITS_SIX_SEAT_REVIEW_2026-07-28.md`.
> - **P0 tripwire: zero lost writes** — and the 2026-07-28 review found three silent ways it could fail (late-sync collision, photo-wedge, lie-fi) plus zero server-side eyes. CC-29/CC-30 close them BEFORE first field use — that is the whole point of using the delay.
> - **Email still sandboxed — gates nothing** (D15 unchanged). Canonical plan: this box → the packets file · charter: `AHITS_PILOT_CHARTER.md` (metrics window now floats per D17).

_Last updated: **2026-07-30 (UXP-1 NAV TRUST — two PRs OPEN + CI GREEN on `development`, UI-layer-only, awaiting Max's evening staging smoke then merge (D16). **PR-1 #232 (1a·1b·1g):** 1a five-tab bottom nav (Map → drawer; minWidth:0 override so 5 tabs fit 320–430px) — records **D30, supersedes D28**; 1b quiet + auto-apply SW update prompt (hadController first-install guard, lifted/dismissible/auto-hide/re-offer, inherit color; `sw.ts` untouched) — records **D31**; 1g phantom-sync guards (flush early-return + banner no-longer-keys-on-syncing) kills the "Syncing 0…" flash. **PR-2 #233 (1c·1d·1f):** 1c operator shell by device-class (coarse-pointer OR down('lg'); admin keeps down('md')) + manifest portrait-primary — records **D32**; 1d mobile-first pre-mount + branded tokens-sourced error/global-error/not-found/loading boundaries with Reload+Home escapes; 1f DetailDrawer renders below the AppBar + built-in 44px close X (removed the vehicles/maintenance/projects page-local X's that hit-tested to the bell). **1e (back-button history guard) DROPPED** (the designated drop) → its own next session, still **P1 (finding 1.5)**. **D30/D31/D32 are cited in code + PR bodies only — Max pastes them into `DECISIONS.md` after his smoke; do not write them.** Both PRs: tsc + eslint + `test:ui` + `next build --webpack` green locally, CI green. Handoff: `AHITS_SESSION_HANDOFF_2026-07-29_UXP1.md`. Packet: `AHITS_UX_PACKETS_2026-07-29.md` (UXP-1). Prior same window: CC-34 maintenance-speaks ✅ FULLY LANDED — all three PRs MERGED to `development`/staging (PR-1 #226 + PR-2 #227 session 1; PR-3 #229 "schedules become real" session 2), post-merge `deploy.yml` GREEN end-to-end (#230 tip `901f413`; verify/migration-safety/migrate/deploy all success). No migration in PR-3. RESIDUALS (verification, not landing gates): Max's staging smoke of the 3a/3c/3d surfaces + the stale-damage count SELECT (never run pre-merge — the arm is bounded 5 nags/run). 3a Add-scheduled-task dialog (D24 bridge) · 3b stale-damage nag · 3c shop chips + DEPLOYMENT close + stranded-unit fix · 3d operator "In repair" · 3e WORK_ORDER-completed alert. D29 already recorded (session 1); D24 gained a note line. Prior same day: CC-33 simplify & unify — PR-1 #223 (dead-code sweep + forward→operator removal, D21) & PR-2 #224 (one unified Transfer entry, D22) ✅ MERGED to development/staging, both deploys green; D21/D22 EXECUTED; RIDER B #7 SELECT ran read-only → 0 live rows (no drain); the two-phone Transfer smoke is owed (Max, physical). CC-16S #220/#221 landed earlier this window, so the pilot-floor queue is now CC-29✅→CC-32✅→CC-30✅→CC-31✅→CC-16S✅→CC-33✅→CC-34. Prior same day: CC-31 accuracy floor + /admin/pilot dashboard — code COMPLETE as 3 PRs: PR-1 cron-truth on `development` via a branch collision (Max: leave it, smoke retroactively), PR-2 #215 + PR-3 #214 OPEN + green awaiting Max's staging smoke; the green-fix #213 MERGED unblocked a red `development` (account_audit_log teardown + 8 alerts mocks + a make_interval test bind); the CC-31 session then SHEPHERDED CC-32's parked PRs to merge — #210/#211/#212 all MERGED — and discovered a latent swallowed `make_interval(hours)` bug in the cron hold-release. Earlier same day: CC-32 friction & flow — PR-1 the D11 glossary sweep MERGED #209; PR-2 #210 flow/friction + PR-3 #211 reach/ergonomics OPEN, code-complete and locally green, blocked only by a red `development` whose cause is CC-31's `tests/setup.ts` teardown gap — routed to CC-31; D11 marked EXECUTED with Max's confirmed word list, D28 six-tab bottom nav recorded). Earlier same day: CC-30 ops floor MERGED — PRs #204 pipeline hardening + #206 server-side eyes to development/staging; both deploys green end-to-end incl. the first-ever migration-safety + env-drift executions; branch protection on `development` live with all three CI checks required and approvals deliberately at 0; `deploy-staging` label deleted — Max checklist items e/f/g DONE, a/b/c/d/h/i still open). Earlier same day: CC-29 offline trust floor MERGED — PRs #202/#203/#205 to development/staging, pre-launch tier 1 complete; D26 late-sync date semantics + D27 sliding session renewal recorded; on-device staging smoke is the remaining pre-first-operator gate). Earlier same day: six-seat critical review + owner interview: pilot postponed→rolling (D17), staging-is-home (D16), CC-16 split/parked (D18), CC-17 full-replace-Clockify with riders (D19), Stewart admin #2 (D20); pilot-floor packets CC-29/30/31/16S written and queued; iOS A6-Lite retro-recorded PASSED; review doc + packets committed; second interview pass D21–D25: Forward→Operator removed + dead-code sweep + unified Transfer entry + NS-9 trigger met + CC-21 spike CLOSED (mounted units = template config, no model) + NS-1 evidence probe RETIRED; CC-33 packet queued)**. Prior: 2026-07-22 (launch-week docs sync). If this date is more than a session old, trust the code and `git log` over this file, and update it._

> **Before re-opening any settled "should we…" question, read `DECISIONS.md`.** The big ones (prod deferred, Map scope, admin exclusion, W0-10 sequencing) are settled — don't re-litigate them.

## 1. One-paragraph state
AHITS is at the pilot doorstep, running on **staging**. The hard architecture is done and audit-verified. Merged to `development` (→ staging): CC-01 through CC-11, Wave-0 patches, and the doc-cleanup corpus. **CC-11 admin-as-operator shipped (PR #181, `a97079b`):** an admin can now hold a rig, be a transfer/handoff recipient, and accept transfers/handoffs — all writing `deployment_assignments` PRIMARY rows, never the legacy column; admin-held rigs are flagged distinctly (not excluded outright) in the missed-check cron and both dashboard surfaces, and excluded from payroll attribution per D3 via a `role`-carrying hook — D3's note makes this a HARD acceptance criterion on CC-17. **CC-22 pilot ops rider shipped (PR #182, `d3d1537`):** a cron dead-man heartbeat (external `CRON_HEARTBEAT_URL` ping + in-app `notification_config.cronLastRunAt`; a `CRON_SILENT` alert raised on the admin-alerts read path when the cron goes quiet >30 min, re-armed by the next successful run) and manual runtime Sentry wiring (server reads `SENTRY_DSN` in `instrumentation.ts`; DSN + `x-request-id` passed to the client via a provider — no `NEXT_PUBLIC_`, no hardcoded DSN; CSP `connect-src` extended for the Sentry ingest hosts). Both no-op cleanly when their secret is absent. The two staging secrets (`AHITS_CRON_HEARTBEAT_URL`, `AHITS_SENTRY_DSN`) are provisioned. **CC-23 design-system substrate shipped (PR #183, `200e528`):** one `src/theme/tokens.ts` (palette/type/spacing/density) consumed by the MUI theme and the rogue raw-HTML surfaces (`~offline`, `s/[token]`); an AA amber contrast fix (`#9a5b00`, 5.4:1, replacing the WCAG-failing `#ff8f00`); the StatCard icon-tint alpha fix; a no-hex ESLint rule (color-hex outside `tokens.ts` + the CC-27-pending allowlist driven to **0**); and the first 3 primitives — DetailDrawer (adopted on all 5 admin drawers, each keeping its width), StatusChip v2 (semantic status mode unchanged + new dense free-label badge mode), BannerStack (collapse-to-one, full-bleed AppShell slot) — plus a jsdom + RTL component-test harness (`npm run test:ui`, wired into the CI verify job). **CC-24 addendum shipped (PR #184, `51a459f`):** three My-Deployment Android device-pass fixes (responsive action row, consistent kit-type chips, 44px remove-tap targets). **CC-24 subtraction + glossary shipped (PR #185, `2861247`):** merged the two operator dashboard cards that both landed on `/operator/scan` (redirect kept); one verb for Dismiss/Revoke (maintenance chip → "Dismissed"); merged the two my-deployment remove-gear flows so the per-row ⊖ opens the shared DispositionDialog pre-filtered to one item (2-tap parity; consumable Log Usage kept separate); made the deployment note optional at launch + kit mutations (5 server zod + clients relaxed, one-tap presets added, a latent empty-note 400 fixed); renamed the MATERIAL request "Mark Fulfilled" → "Mark Handled" (it moves no stock — "Fulfill" reserved for the stock-moving reservation flow, untouched); and swept Staged/Prepared → "Staged". The Fulfill/Pick-up/Check-out/Claim glossary cluster is **deferred to CC-14** (see D9). **CC-25 live-camera QR shipped (PR #186, `34ba13b`):** replaced the photo-capture→jsQR flow with a continuous `getUserMedia` viewfinder in ONE shared `QrScannerDialog` consumed in all three operator scanners (operator/scan + the two my-deployment kit scanners) — BarcodeDetector where available else jsQR capped ≤8/sec on a ≤640px downscale (`src/lib/qr-scan.ts`); honest distinguished failures ("not found" vs "can't verify right now" — the "failed to process image" lie is gone); manual entry; torch where supported; permission-denied/no-camera → relocated photo-capture fallback; stream stops on close/tab-hidden and resumes on app-switch-back. Every per-site decode loop + manual handler deleted. **This completes the reflection-increment sequence (CC-22 → CC-25).** **CC-12 structural-performance shipped as three PRs (approved scope: my-deployment only):** **PR1 (#187, offline-queue UX)** — an OUTBOX view rendering the offline queue's per-item labels + `lastError` with per-item retry/discard (replacing the blind bulk "Dismiss"), and a 401-parked-queue banner ("Session expired — sign in to send N saved actions" + login link, driven by a new `sessionExpired` signal on `useOfflineQueue`, which also gained `listAll`/`retryItem`); **PR2 (#188, SWR)** — a `useFreshList` SWR hook (`revalidateOnReconnect`, tracks `updatedAt`) + a shared `FreshnessIndicator` ("Data as of HH:MM"), landed on the stable `operator/requests` list (deliberately NOT on a monolith about to be split); **PR3 (#189, the split)** — the two heaviest My-Deployment cards (Vehicles, Kit) extracted into `React.memo`'d children in `DeploymentCards.tsx`, container thinned to `useCallback` handlers + a **memoized `kitItems`** so the memo boundary actually holds in production (a fresh array each render would have silently broken it — CI didn't catch it, the fix did). Every split/new component ships a component test (`npm run test:ui`, 34/34); memo boundaries are proven by a leaf render-count spy (React.Profiler rejected — it fires on every parent re-render regardless of child bailout). **The admin/deployments + admin/inventory splits are DEFERRED — demand-pull, owner CC-18 / first packet that touches them (D10).** **CC-14 "Today" view shipped as PRs #190–#193 (+ #194 test fix), live on staging:** the static operator dashboard is replaced in place by the operator's day (NS-10) — one `GET /api/operator/today` aggregate (pure read-side over Rig/RigVehicle/DailyCheck/TransferRequest/DeploymentHandoff/DeploymentRequest, added to the SW field-reads cache) consumed via `useFreshList` ("Data as of HH:MM"), composed into: the day's one contextual action (Start daily check → You're set), Awaiting-Pickup cards (CC-09), transfers/handoffs waiting on me, current deployment + project + per-vehicle done/due one-tap checks, my open requests, an `EmptyState` for the not-deployed case, the kept CC-24 Scan card, and a code-level slot for the post-Time/Invoicing clock. **Daily-check gained** an NS-5 inline odometer sanity warning (warn, never block), passive time-to-complete (`DailyCheck.durationMs`, nullable additive migration), and the Pilot Charter **metric-1 adoption denominator** (`getDailyCheckAdoption` / admin-only `GET /api/admin/pilot-metrics` — eligible checks / deployments). **Operator IA:** Requests added to the bottom nav, the transfer/handoff pending badge moved onto the bottom-bar My-Deployment tab (shared `useIncomingPendingCount` hook), a confirm dialog on request Cancel, and a shared `SearchableSelect` swapped into the roster/item dropdowns. **PRE-FLIGHT confirmed `Rig.fromRequestId` already shipped in CC-09** (no schema fix needed). **D5 (pilot gate) is effectively Option A — this packet is the gate — but Max left it uninitialed; pilot start = CC-14 on staging AND charter signed AND A6 pass green.** **The D9 glossary sweep (PR5) was cut (session length) and re-deferred as D11** — owner: next session after CC-14, strings-only, precondition met. **CC-26 read-only daily-check viewer shipped (PR #195, live on staging) — the pencil-whipping falsifier, pulled forward from CC-20 #1, a pilot-fortnight gate:** a new `GET /api/daily-check/[id]` (admin any / operator own) + a read-only `DailyCheckViewer`/`DailyCheckDetails` showing a submitted check's FULL contents — checklist answers (failures highlighted), odometer, site, issues, time-to-complete, photos via `PhotoGallery`, and a clearly-marked **GPS slot** (check-level absent-safe until CC-15 adds columns; per-photo `gpsLat/lng` rendered when present); nothing writable. **THE ONE WRITE:** `checkId` added to `DAILY_CHECK_FAILED` alert metadata, and `createAlert`'s dedup `update:{}` → `update:{ metadata }` so a re-raise points the link at the LATEST check (metadata-only → no re-notify; safe across all alert types). **Deep-links** via the single `alertLink` chokepoint (dashboard/bell/email): FAILED → `/admin/vehicles?check=<id>` (graceful fallback for stale/pre-CC-26 ids), MISSED → `/admin/deployments?operator=<id>` (opens that operator's rig drawer — a MISSED alert has no vehicle record); every other `alertLink` branch byte-identical (**CC-20 #6 stays PARKED**). **Reachability:** the vehicle drawer's existing recent-checks list is EXTENDED into clickable rows (per the pre-flight — no duplicate list); the deployment drawer gets a per-vehicle "View checks" → the vehicle's history → viewer (transitive by design, D12). No migration (alert metadata already `Json?`). **CC-27 FulfillmentChecklist re-skin shipped (PR #196, live on staging) — the pilot-fortnight filler:** rebuilt the raw-HTML/hardcoded-hex/system-font FulfillmentChecklist (an entire parallel UNTHEMED design system inside the themed admin app — the most visible "two apps in one page" spot, in admin/requests) on **MUI + the tokens-sourced theme** (Button/TextField/MenuItem + StatusChip badges; themed primary/info/error/default — no raw hex, no system-font). **Behavior-identical re-skin** (logic/state/handlers/props/interface byte-identical; `deltaLine`/optimistic-update copied verbatim; Select empty-value semantics, serialized needs-unit gate, qty clamp, stage-gating, per-form errors, disabled-during-load all preserved). Removed the file's CC-23 no-hex lint-allowlist entry (the hex rule now enforces it; `src/app/s/**` kept — CC-23 item 1 owns the portal re-palette). 6 parity component tests. **MERGE GATE MET:** the authenticated hub-flow staging smoke passed — fulfilled a 2-line reservation end-to-end through the rebuilt checklist on admin/requests (Edit→Adjusted+delta, Deny validation + real deny, progress→green "Ready to stage", Stage→"Reservation staged"), test data cleaned up. **Render consumers are admin/requests + the s/[token] portal** (operator/requests only references it in a comment — my PR body's "3 consumers" was corrected); the portal font shifts system-ui→Inter, a known delta CC-23 item 1 subsumes. Production is deliberately deferred (see D1). **Pilot charter SIGNED (2026-07-20):** 9 operators, project **CUL005**, D5=**Option A** (CC-28 permanently moot), D7 second contact = Stewart Arbuckle; D6 sandbox-flip rule confirmed (execute on start date, not before). **`AHITS_PILOT_CHARTER.md` is filled + signed; D5/D6/D7 in `DECISIONS.md` are now ACTIVE/RESOLVED.** **🟡 PILOT GO PENDING ONE GATE — start Monday 2026-07-27 (contingent).** Green: charter signed (2026-07-20) + A6-Lite **Android** (2026-07-20, rows 2/5/6/19/23). **Outstanding: the A6-Lite iOS pass** (installed PWA, rows 2/5/6/19/23/29) — D13 was amended 2026-07-20 because the cohort now includes iOS operators. The full A6 matrix stays parked to pre-CC-17 (edge rows 20–22 + all-column ceremony). **The sole outstanding pre-start item is the iOS A6-Lite pass (§3)** — Max's escalation phone is now filled. **CC-15 Deployment Map SHIPPED (D14, 2026-07-21) — inside the Thursday 07-23 freeze:** **PR-1 (#197, merged):** additive `DailyCheck.gpsLat/gpsLng/gpsAccuracy` migration + resolve-or-skip GPS capture in the daily-check `buildPayload` (denied/dismissed/timeout → submits with null coords, never blocks; coords ride the offline queue). FND-35 confirmed already-fixed. **PR-2 (#198, merged):** the read-side maps — admin pins (recency-coloured green/amber/red, businessDate-aware) with route-history trail + `?operator=` deep-link, and an operator crew map of teammates' last-known positions (D2: no live tracking anywhere; `AHITS_MAPBOX_TOKEN` secret ENABLED + mounted; the drawer→route-history is a `/admin/map?rig=<id>` link, D12 transitive-reach — Max accepted). **Smoke passed on staging** (3 recency-coloured pins + trail + crew map), via `scripts/cc15-staging-smoke-seed.sql`/`-cleanup.sql` (staging-only, id-keyed `seed-cc15-*`, since the POST clamps `date` to today; **all synthetic rows cleaned up, 0 remain**). **Step 7 (weather stamps) OUT** (post-launch). A6-Lite **row 29** (location grant AND deny at the daily check) added — iOS A6-Lite re-run on the post-merge build includes it. **NOTE:** `AHITS_MAPBOX_TOKEN` is now mandatory for every staging deploy; prod cutover (D1) will need `AHITS_PROD_MAPBOX_TOKEN`. **Copy-link invites SHIPPED (PR #200, merged):** email-independent onboarding — `POST /api/users/invite` + the resend route take `delivery: 'EMAIL'|'LINK'` (default EMAIL, byte-identical); LINK skips `sendEmail` and returns `{ setupUrl, expiresAt }` (`Cache-Control: no-store`; raw token never persisted/logged/audited), same token-hash/48h-expiry/revoke, new audit actions `INVITE_LINK_CREATED`/`INVITE_LINK_REGENERATED` (no migration — `action` is a `String`). Team Management gains an email-vs-link choice, a one-time copy dialog, and a net-new Pending Invites list (Regenerate link / Revoke). Operators still set their own PIN on `/setup-account`. Revoke-deadness verified at the account-minting `/complete` route (not just `validate`). Staging-smoked (incognito setup + regenerate → old link 404s). **CC-30 ops floor SHIPPED (PRs #204 + #206, 2026-07-28) — D16 staging-is-home made operational:** the `deploy-staging` label path onto the live service is gone (`pr-staging-deploy.yml` is dispatch-only, requires an explicit non-live `service`, hard-fails on `ahits-web-app-staging`, and no longer migrates); `migration-safety` now gates `deploy.yml` between verify and migrate so a direct push can't bypass it, and an acknowledged destructive migration is a hard fail on `development` too; the W0-10 DROP guard covers development-targeting PRs; `db-seed`/`db-reset`/`prisma/seed.ts` refuse a live Supabase host without `AHITS_DANGEROUS_TARGET=yes-i-mean-staging`; `EMAIL_SANDBOX` defaults true, `EMAIL_SANDBOX_TO` is set (its absence had made every sandboxed send a silent `SKIPPED` that never raised `EMAIL_FAILED`), staging runs `MIN_INSTANCES=1`, and a post-deploy env-drift check fails the workflow if any of the three revert. Server-side eyes: Sentry captures at the offline queue's possible-data-loss tripwire and all six `-> 'failed'` transitions (labels/endpoints/status codes only — never payload bodies, photos, or device filenames), the cron's drift + INV-1..5 bare catches now log + `captureException`, and an advisory-lock **connect error** returns 500 (`advisory-lock-connect-error`) instead of looking like benign contention forever. New `PILOT_ROLLBACK.md`. Branch protection on `development` is live (PR required + all three CI checks). Handoff: `AHITS_SESSION_HANDOFF_2026-07-28_CC30.md`. **CC-32 friction & flow SHIPPED (PRs #209/#210/#211/#212, 2026-07-28):** D11 glossary sweep, one-time failure entry, site prefill, 2-step deployment builder, template-race fix, GPS warm-capture, openable Outbox, honest copy, Map bottom-nav tab (D28), 44px pass. **CC-31 accuracy floor + /admin/pilot dashboard — code COMPLETE (2026-07-28):** PR-1 (cron truth — INV-5 false-alarm fix + alert auto-resolves) is on `development` (`616cc6e`, landed via a branch collision, green after the #213 test-fallout fix); PR-2 (#215 — expired-link unjam, `forOperatorId` scope + read-only Cancel gating, durationMs honesty, request-scope indexes) and PR-3 (#214 — `/admin/pilot` dashboard + metrics API with snapshot-per-day eligibility, GPS grant rate, duration buckets + `<20s` flag, per-rig breakdown) are OPEN + green, awaiting Max's staging smoke. Handoff: `AHITS_SESSION_HANDOFF_2026-07-28_CC31.md`.

## 2. Environments
- **development → staging:** the live working line. Has the full Wave-0 hardening + W0-10 through PR-4a + CC-01 through CC-11 + CC-22 + CC-23 + CC-24 (addendum + subtraction/glossary) + CC-25 + CC-12 (PRs #187/#188/#189) + CC-14 (PRs #190/#191/#192/#193 + #194 test fix) + CC-26 (PR #195) + CC-27 (PR #196) + **CC-15 (PRs #197 GPS capture + #198 maps)** + **copy-link invites (PR #200)** + **CC-29 (PRs #202/#203/#205)** + **CC-30 (PRs #204 pipeline + #206 server eyes)** + **CC-32 (all four: #209 PR-1 · #210 PR-2 · #211 PR-3 · #212 docs)** + **CC-31 (PR-1 `616cc6e` cron truth · #214 PR-3 · #215 PR-2 · #213 green-fix — all MERGED, preview-smoked)** + **CC-16S (#220 TTL-warmup + #221 read-after-leak)** + **CC-33 (#223 dead-code + forward→operator removal · #224 unified Transfer — both MERGED 2026-07-29, staging deploys green; two-phone Transfer smoke owed)**. **Smoke on staging after every merge.**
- **production:** the git branch is now *current* (PR #144 merged 2026-07-11, `c0d231b`) but there is **NO prod environment** — no Supabase project, no `AHITS_PROD_*` secrets — so nothing is deployed and the triggered deploy failed harmlessly. **Prod is deferred; see `DECISIONS.md` D1 before touching anything prod.**

## 3. Active work — in flight right now
- **UXP-1 NAV TRUST — two PRs OPEN + CI GREEN on `development`, awaiting Max's evening staging smoke then merge (D16), 2026-07-30.** From `AHITS_UX_PACKETS_2026-07-29.md` (UXP-1) / review `AHITS_UX_SIX_SEAT_REVIEW_2026-07-29.md` §1. **UI layer only — no schema, no `sw.ts` change, no new deps.** Both branched off `development`; files disjoint (merge order-independent).
  - **PR-1 #232** `feature/20260729/Agricarbon-USA-uxp1-nav-trust-pr1` (tip `1d7b4d2`) — **1a** five-tab bottom nav (`OperatorBottomNav.tsx`: Map removed from bar, still in the "Crew Map" drawer entry; `minWidth:0/px:0.5` override so 5 tabs fit 320–430px; test snapshots the label set so a 6th tab fails loudly) — **records D30, supersedes D28**; **1b** quiet + auto-apply SW update prompt (`ServiceWorkerUpdater.tsx` only; `hadController` first-install guard, lifted above the tab bar, dismissible + ~8s auto-hide + visibility re-offer, `inherit` color; **`sw.ts` untouched**) — **records D31**; **1g** phantom-sync guards (`useOfflineQueue.ts` `flush()` returns before `setSyncing(true)` when no non-failed items; `OfflineBanner.tsx` queue banner requires `pending>0||isOffline`, never `syncing` alone) — kills the "Syncing 0 action(s)…" + header-spinner + layout-shift flash (E1). CI green (verify Tests + Lint/type-check/build + migration-safety + DROP guard).
  - **PR-2 #233** `feature/20260729/Agricarbon-USA-uxp1-nav-trust-pr2` (tip `fc5b0f2`) — **1c** operator shell by DEVICE CLASS (`AppShell.tsx` `shellMode` prop; operator = coarse-pointer OR `down('lg')`, admin keeps `down('md')`) + `manifest.json` `portrait-primary` — **records D32**; **1d** pre-mount default flips to the mobile composition (SSR + first client render both mobile → hydration matches; desktop upgrades post-mount) + new branded, tokens-sourced, offline-tolerant `error.tsx`/`global-error.tsx`/`not-found.tsx`/`loading.tsx`×2 with Reload + Home/Dashboard escapes; **1f** `DetailDrawer.tsx` renders below the AppBar (top offset + reduced height) + built-in 44px close X — removed the page-local X's on **vehicles/maintenance/projects** that hit-tested to the notification bell (all 5 DetailDrawer adopters inherit the close; **`admin/users` is Dialog-based, no drawer — §1.6's "users" line is stale**). CI green; `next build --webpack` compiles (`/_not-found` in manifest).
  - **DECISIONS — Max writes these, not the session:** **D30** (five-tab nav, supersedes D28), **D31** (quiet update prompt), **D32** (device-class operator shell) are cited in code comments + the PR bodies ONLY. Max pastes the `DECISIONS.md` entries after his smoke and marks D28 `Superseded-by: D30`.
  - **DROPPED — 1e (back-button history guard), still P1 (finding 1.5):** `useHistoryGuard` (Back closes dialogs/drawers, steps the daily-check wizard back) was the designated drop — its risk concentrates in `popstate` handling of the daily-check wizard, which jsdom can't verify, so rushing it at session tail is the D11 anti-pattern. Owner: its own next session (scope: daily-check wizard + DetailDrawer + transfer/handoff respond dialogs + RequestComposer). Do NOT fold into "UXP-1 done."
  - **SMOKE-OWED (Max, staging evening — not scriptable in jsdom / no local DB):** 1a five tabs visible at 320/375/390/430; 1b no toast on fresh install + dismissible-above-bar on a deploy; 1c 932×430 + 926×428 keep the bottom-nav shell (no drawer), rotate ≠ swap; 1d JS-blocked load shows nav + hamburger, **no dev-console hydration-mismatch warning**, branded 404 + error page with working escapes; 1f all five drawers' headers + X visible and `elementFromPoint(X)` returns the X not the bell (+ the X on a long drawer). See the handoff for the full list.
- **CC-34 maintenance-speaks — ✅ FULLY LANDED (all 3 PRs MERGED to `development`/staging), 2026-07-29.** **PR-1 (#226) + PR-2 (#227)** (session 1) and **PR-3 (#229 "schedules become real")** (session 2) all merged; the post-merge `deploy.yml` ran GREEN end-to-end (tip `901f413`). PR-3 was a standalone branch off `development` (not stacked — 1b's `rigId` + the `returnDestination*` columns were already live). **PR-3 (#229) — full CI green at merge** (migration-safety: no migration to scan; `verify / Tests` ran the new node DB suite `tests/cc34-schedules-and-alerts.test.ts`; Lint/type-check/build pass). **RESIDUALS (verification, not landing gates):** Max's staging smoke of 3a/3c/3d + the stale-damage count SELECT (never run pre-merge; the cron arm is bounded 5 nags/run — see #229 body for the query). **No migration** (reuses PR-1b `rigId` + existing return-destination columns → previewable). **3a** Add-scheduled-task dialog = the first UI caller of `POST /api/maintenance` (D24 bridge — named Wintex/Giddings rows enterable); SearchableSelect subject, MILEAGE/DAYS/MONTHS only (PER_DEPLOYMENT NOT offered — D29-4; zod enum dormant), native `type="date"`; server defaults nextDue/nextOdometer from the interval so a schedulable task is never born unschedulable. **3b** stale-damage cron nag (isDamageReport, >7d untouched → MAINTENANCE_OVERDUE + `staleDamageDays`, bounded `orderBy updatedAt asc take 5`; copy "no updates in N days. Worth a look."). **3c** two-click shop chips + close-dialog DEPLOYMENT return destination + the **stranded-unit server fix** (open kit item on active rig → restore CHECKED_OUT not AVAILABLE, both restore paths — RIDER C #10). **3d** `?rigId=` filter + read-only "In repair — <status>" caption on My-Deployment (human label via `lib/status.ts`, no chip+caption stacking; repair maps memoized). **3e** WORK_ORDER COMPLETED → real deduped DAMAGE_REPORTED READY_TO_FINALIZE alert (resolve-then-create post-commit) instead of a scroll-past bell. **OWED before merge (Max):** the pre-merge stale-damage count SELECT read-only vs staging (query in PR #229 body; if >15 triage before deploy), then evening staging smoke, then merge; **mark CC-34 fully landed in STATUS §1/ledger only after #229 merges.** Handoff: `AHITS_SESSION_HANDOFF_2026-07-29_CC34-session2.md`. **Session-1 detail (both now MERGED):** **PR-1 (#226) "deployment drawer tells the truth" (R3 + orphan closure)** — CI fully green incl. the node DB suite and the migration-safety gate, which **SCANNED** the one additive migration (nullable `rigId`+`reportedById` on `maintenance_tasks`; RIDER C SRE confirmation met). 1a equipment-health chips + open-task badges (RIDER C: drawer did NOT actually fetch `vehicle.status` — fixed; fetch-on-open added); 1b tasks carry rig+reporter; 1c every status-flip branch now creates a task and/or rings/clears the bell (consumable activeKey trap avoided; maintenance DELETE resolves alerts). **D29 appended to `DECISIONS.md` in PR-1** (session-split: at PR-1 close for durability). **PR-2 (#227) "one way to report a problem" (R2 + check dead-end)** — stacked on PR-1 (uses 1b's columns), retargeted base→`development` so CI runs (its diff includes PR-1's commits until #226 merges — **merge #226 first**). 2a shared `ReportProblemDialog` (annotation not removal; 4 trigger sites; both routes `withIdempotency` + photo-422 backstop; vehicle route default = flip-when-absent; my-deployment net 0); 2b field-fix reaches items + idempotency; 2c admin check→task promotion (`api/daily-check/[id]/open-task`, reportedById=operator, resolves the failed-check bell, no double DAMAGE_REPORTED). Two node tests. **SESSION 2 = PR-3 (schedules — the value center); do it as its own session.** Handoff: `AHITS_SESSION_HANDOFF_2026-07-29_CC34.md`. Owed: Max's per-PR smoke (PR-1 first — do NOT preview PR-1, previews don't migrate; airplane-mode smoke is PR-2's Antagonist gate).
- **CC-33 simplify & unify — ✅ MERGED to `development`/staging (PR-1 #223 + PR-2 #224), 2026-07-29; both staging deploys green end-to-end. D21/D22 EXECUTED.** RIDER B #7 pre-merge check ran read-only against staging → **0 live FORWARDED-to-operator rows** (no drain needed); #223 merged (deploy green), then #224 branch-updated → green → merged (deploy green). **REMAINING (Max, physical): the two-phone Transfer smoke on live staging** (both choices; entire-rig accept → recipient becomes holder; selected-gear accept → item moves, deployment stays). Both cut off `development` (post-CC-16S `48b8fed`); disjoint files; PR-1 landed first. **PR-1 (#223) — dead-code sweep + forward→operator removal (D21):** deleted `lib/shipments.ts` (D23: model/enum stay dormant), 7 dead assignment fns (kept `getDeploymentRosters`), `lowStockByHub`, `getAuthorizedRig`, 8 caller-less email templates, 4 dead utils + the orphaned `relativeTime` plugin, `api/inventory/stock/route.ts`, checkout's dead GET, `@emotion/cache`/`@emotion/server`/`@mui/x-date-pickers` + the LocalizationProvider wrapper, two unused icon SVGs, `batch6a-date-unify.patch` (landed by hand into `admin/hubs`); removed the never-used Forward→Operator branch (write side + UI; `complete` now admin-only; read side of stale FORWARDED rows byte-untouched). Every zero-importer claim re-proven with word-boundary greps (Antagonist seat). E6 test rewritten (former-fulfiller/requester/unrelated → 403; admin → 200) — **CI `verify / Tests` confirms it.** **PR-2 (#224) — one unified Transfer entry (D22, client-flow only):** new `TransferEntryDialog` (Entire rig / Selected gear router) + extracted `EntireRigTransferDialog`; `my-deployment/page.tsx` 1,924 → 1,905 (−19, anti-regrowth met); full handoff→transfer string sweep driven by `grep -in "handoff"` → 0 operator-facing literals; server models/routes byte-untouched. 7 new component tests (test:ui 23/115). **OWED before done:** Max's staging smoke on both; RIDER B #7 (drain any live `FORWARDED + fulfillerOperatorId` row before PR-1 merge); on merge mark D21/D22 EXECUTED + flip the ledger/TODO. Handoff: `AHITS_SESSION_HANDOFF_2026-07-28_CC33.md`.
- **CC-31 accuracy floor + pilot dashboard — ✅ FULLY MERGED to `development`/staging (PR-1 + #214 PR-3 + #215 PR-2 + #213 green-fix), 2026-07-28. Preview-smoked before merge.** **Smoke results:** PR-3 `/admin/pilot` eyeballed end-to-end on a preview (adoption/avg-time/GPS tiles, snapshot-per-day eligibility visibly `/3` vs `/4`, `<20s` flags, GPS math, per-rig grid, and a check row → CC-26 viewer deep-link opening the exact check); PR-2 **item 3 verified live** — the "Expired link" chip rendered on a real expired HUB_RETURN link and **Mark received succeeded on the first click** (the `bypassExpiry` fix — previously a 409), INBOUND 3→2. PR-2 items 4/5/7 (operator-side read-only Cancel gating, durationMs-before-GPS, indexes) are CI-covered (would need an operator PIN / submit-timing to eyeball). **RESIDUALS:** (a) delete the two preview services `ahits-web-app-preview-cc31-pr3` / `-pr2` (`gcloud run services delete … --region us-central1`); (b) **PR-1 still owes its retroactive staging smoke** (cron force-run + alert bell — it bypassed the gate via the branch collision).
  - **PR-1 (cron truth) — on `development` (`616cc6e`), green after #213.** INV-5 false-alarm fix (age-qualify + NOT-EXISTS for in-flight HUB_RETURN link / PENDING transfer) + alert auto-resolves (PIN_LOCKED, MATERIAL_REQUEST via the `applyRequestTransition` wrapper, EQUIPMENT_NOT_RETURNED, INVENTORY_DRIFT INV-1..5 + per-item — every resolve only in the `else` of a query that SUCCEEDED). **It landed directly on `development` via a branch collision** (a parallel CC-32 session moved HEAD to `development` mid-commit); Max's call was to leave it and smoke retroactively (protected branch blocked a clean rewind). **Owed: PR-1's retroactive staging smoke** (cron force-run + alert bell).
  - **PR-2 (#215, open + green):** expired HUB_RETURN unjam (admin receive `bypassExpiry`, state gate untouched, public path still 409s; "Expired link" chip) · `forOperatorId` in `listRequests` + **client Cancel gated by `requestedById`** (finding: server already 403s a non-requester; the button was the gap) · durationMs stamped before the GPS wait · two additive indexes + migration. Merged up to current `development`; the one overlap (`daily-check/page.tsx` vs CC-32 GPS warm-capture) auto-merged and verified sound.
  - **PR-3 (#214, open + green):** `/admin/pilot` dashboard + metrics API — **snapshot-per-day eligibility (6b, no retroactive shift)**, GPS grant rate, duration buckets + `<20s` flag, the day's checks list (deep-linking the CC-26 viewer, D12), per-rig operator+vehicle breakdown (D3 `isAdminHeld`), nav 'Pilot' admin-only.
  - **The green-fix (#213, MERGED):** development was red from PR-1's test fallout — three causes, all fixed: `account_audit_log` missing from the `setup.ts` teardown (the routed one-line fix), 8 `alerts` mocks missing `resolveActiveAlert` (my end-route/`applyRequestTransition` calls broke them), and a `make_interval(days=>bigint)` bind in my INV-5 test. **`development` is green.**
  - **Non-goals held:** NO EXPIRED-link persistence sweep (PARKED — trigger: expired-link volume); NO secondary-operator Today rework (own pre-CC-17 packet — item 4 was only the scope line + read-only gating). **Discovered latent bug (out of scope, flagged):** the cron's step-7 hold-release `make_interval(hours=>$1)` throws `42883` but is swallowed by its try/catch, so stale-hold release has silently no-op'd — needs its own fix.
- **CC-32 friction & flow — ✅ ALL MERGED (#209 PR-1 · #210 PR-2 · #211 PR-3 · #212 docs), 2026-07-28.** Shepherded to merge by the CC-31 session after `development` went green (CC-32's session had closed): #210 branch-updated → green → merged; #211 retargeted to `development`, a one-line `OfflineBanner` conflict resolved (kept its 44px thumb hit-area) → green → merged; #212 docs merged. Original detail below.
- **CC-32 (pre-merge detail) — PR-1 ✅ MERGED (#209 `31f0d9d`); PR-2/PR-3 were OPEN+BLOCKED on the red `development` now fixed, 2026-07-28.** All three are code-complete and locally verified (`tsc` clean · `eslint src` 0 errors · `test:ui` **22 files / 108 tests**, up from 18/84). **PR-1 (merged):** the D11 glossary sweep — Max confirmed the word list verbatim, see **D11 EXECUTED**. **PR-2 (#210, open):** one-time failure entry (the issue summary seeds from the per-item "No" notes); site prefill via an additive read-side `lastCheckSite` on `GET /api/vehicles/[id]` (**the one permitted API change — no schema, no migration**); handoff note optional + presets (server zod relaxed in the same PR, CC-24 item-4 precedent); the deployment builder cut **4 steps → 2**; the **antagonist-seat template-race fix** (see below); GPS warm-capture on the review step (up to 10 dead seconds off an ~11-second check, semantics unchanged — still one fix, still resolve-or-skip, still never blocks, D2 intact); the Outbox made openable from the merely-*pending* banner (P0-3 operator half, queue engine untouched); and the copy-honesty pass (the iOS PWA has no pull-to-refresh gesture; the empty state no longer invites a second-seat operator to start a duplicate deployment). **PR-3 (#211, open, stacked on PR-2):** Map promoted to a 6th bottom-nav tab (**D28**) with the offline half (`sw.ts` exact-match `/api/map/crew`; `CrewMapView` on `useFreshList` + `FreshnessIndicator`), the 44px pass on CC-14's Today leaves, and the residual reach sweep with a fix/leave call recorded per finding.
  - **Antagonist-seat outcome (PR-2 item 2.5) — TWO packet defects fixed and a THIRD found.** (a) `handleReset` never re-resolved the template (`[vehicleId, selectedVehicleType]` both unchanged), so **check #2 of the day silently ran the built-in 16 items instead of the admin template** — fixed with a **fetch-nonce** in the effect key. (b) A late template response could wipe answers already given (the FND-35 family) — fixed with a pristine guard; touched → answers kept + a dismissible notice, no merge UI. **(c) The new one:** implementing the packet's predicate literally (*touched OR past step 0*) **re-broke (a)** — after a reset, an operator who tapped Next before the template landed was "past step 0", the guard blocked the resolve, and the default list came back, now hidden behind the fix. The predicate therefore ships as two independent halves (`rowsTouched`, which always vetoes, and `pastStep0`) plus a **one-shot force flag** for the reset-triggered resolve. A **fourth** interaction was closed too: a deliberate vehicle switch now clears the answers, or vehicle A's answers would ride vehicle B's check. Both required races have tests.
  - **One more self-review catch, worth remembering:** the 2.2 site prefill had the same shape of bug — an **untyped** prefill survived a vehicle switch, so **vehicle B's check could be filed under vehicle A's site with nobody having typed it**, and silently (the helper text compares against B's value, so it showed no cue). Fixed and covered by a regression test **verified to fail without the fix**.
  - **✅ THE BLOCKER — RESOLVED (#213 merged, 2026-07-28).** The `account_audit_log` teardown gap was fixed as routed (plus two more PR-1 test-fallout causes; see the CC-31 §3 entry above). `development` is green; #210 and #211 were then updated + merged with no code change to either.
  - **Process note for the next session:** a parallel CC-31 session was live in this working tree. A `git add -A` on CC-32 PR-1's first push scooped up four of its in-flight files; the branch was force-pushed clean and CC-31's work was left in place, not reverted. **Every later CC-32 commit stages explicit paths — never `-A`.** Handoff: `AHITS_SESSION_HANDOFF_2026-07-28_CC32.md`.
- **CC-30 ops floor (D16 staging-is-home) — ✅ MERGED to development/staging (PRs #204 `08aee2d` + #206 `2a09eac`), 2026-07-28.** Both deploys ran green end-to-end (verify → migration-safety → migrate → deploy → env-drift). Closes review §2. **#204:** the `deploy-staging` label path onto the live service is GONE (`pr-staging-deploy.yml` is `workflow_dispatch`-only, requires an explicit non-live `service`, hard-fails with `REFUSING: pr-staging-deploy may never target ahits-web-app-staging`, and no longer migrates — previews must never touch the pilot DB); `migration-safety` now runs in `deploy.yml` between verify and migrate (a direct push no longer bypasses it) and an *acknowledged* destructive migration is a hard fail on `development` too, so it can only land via a labelled PR; the W0-10 DROP guard now fires for development-targeting PRs (**verified live — it ran and passed on #204**); `db-seed`/`db-reset` and `prisma/seed.ts` refuse a live Supabase host unless `AHITS_DANGEROUS_TARGET=yes-i-mean-staging`; `EMAIL_SANDBOX` defaults true, `EMAIL_SANDBOX_TO` is now actually set (its absence made every sandboxed send a silent `SKIPPED` that never raised `EMAIL_FAILED`), staging runs `MIN_INSTANCES=1`, and a post-deploy **env-drift** check fails the workflow if any of the three revert. New `PILOT_ROLLBACK.md` (traffic-rollback play + never-hand-revert-the-schema + Q1–Q4 lost-write queries). **#206:** Sentry captures at the possible-data-loss tripwire + all `-> 'failed'` transitions (labels/endpoints/errors only — never payload bodies or photos); the cron's drift + INV-1..5 **bare catches** now log + `captureException`; the advisory lock distinguishes contention (200, unchanged) from a **connect error** (500 `advisory-lock-connect-error`) so a dead DB stops looking green to Cloud Scheduler forever. **CC-29 landed first, so both PRs were rebased onto post-CC-29 `development`; #206's captures were re-anchored across the SIX `-> 'failed'` transitions CC-29 now has (it added three — photo retry-cap, that branch's quarantine loop, in-flight-409 retry-cap — which would otherwise have shipped blind). CC-29's `tests/offline` harness passes with the captures in (test:ui 18/84).** Handoff: `AHITS_SESSION_HANDOFF_2026-07-28_CC30.md`.
- **CC-30 live-fire checklist (Max, console — NONE has ever been attempted). Record each result here.**
  - ☐ **a. Backups + restore drill.** Supabase → Settings → Database → Backups: write down the plan + retention actually in effect; enable PITR if the tier offers it. Then ONE drill (~15 min): restore to a **NEW throwaway project** (never over the live one), confirm `daily_checks` rows exist, delete the throwaway. **Write the click-path into `PILOT_ROLLBACK.md`'s "Restore drill" placeholder.** → result:
  - ☐ **b. healthchecks.io.** Confirm a **real ping arrived in the last hour** (CC-22 shipped this; never observed live). Set the grace period **below 30 min** so it alarms before the in-app `CRON_SILENT`. → result:
  - ☐ **c. Dead-man's first live fire.** Cloud Scheduler: pause the cron, wait >30 min → the `CRON_SILENT` banner must appear on the admin dashboard; resume → the next run clears it. *(Supersedes the CC-22 item (c) below.)* → result:
  - ☐ **d. Sentry.** `/admin/settings` → Sentry diagnostics → trigger the client-error and server-error buttons; confirm both arrive **with a `request_id` tag**. *(Supersedes CC-22 item (a).)* → result:
  - ✅ **e. min-instances — DONE (machine-verified, 2026-07-28).** The post-deploy env-drift step ran on BOTH CC-30 deploys (`08aee2d`, `2a09eac`) and passed, which is an assertion — not an eyeball — that the live service has `EMAIL_SANDBOX=true`, `EMAIL_SANDBOX_TO` non-empty, and **minScale = 1**. Any future deploy that reverts one of the three now fails the workflow red. A console glance remains optional confirmation, no longer the only evidence.
  - ✅ **f. Branch protection — DONE (2026-07-28).** `development` now requires a PR + **all three** status checks: `Backward-compatible migrations`, `verify / Tests`, `verify / Lint, type-check & build`. Force-pushes and deletions off; `strict: true` (branch must be current before merge). **Required approvals deliberately set to 0, not 1** — as a solo owner Max cannot self-approve, so `1` would have forced `--admin` on every merge and hollowed out the control it was meant to create. Zero approvals still requires a PR and green checks, which is the protection that matters here. **Caught while configuring:** the initial rule required ONLY `Backward-compatible migrations`, so an approved PR could have merged with failing tests or a broken build; the two `verify` contexts were added to close that. Proven working — #206 sat `BLOCKED` until its updated branch went green, with no override used on either merge.
  - ✅ **g. `deploy-staging` label — DELETED (2026-07-28).** `gh label delete deploy-staging --yes`; verified 0 hits in `gh label list`. It had only ever been applied to two long-merged PRs (#139, #166). The in-repo guard in `pr-staging-deploy.yml` remains the backstop.
  - ☐ **h. Mapbox token.** account.mapbox.com → Tokens → confirm `AHITS_MAPBOX_TOKEN` is a **public (`pk.`)** token with **URL restrictions** limited to the staging domain — it is served to authed clients, so that is the only fence. If unrestricted, add it (no code change). → result:
  - ☐ **i. OPTIONAL (~5 min).** Add a GCP Uptime check on `GET https://<staging-url>/api/health` expecting 200 (it returns 503 when the DB is unreachable). The cron heartbeat proves the *cron* is alive, not the *service*; a wedged app at 5:50am is currently found by operator texts. → result:
- **CC-30 findings a future session must not rediscover the hard way (all four are live, none are blockers).**
  - **`scripts/check-migration-safety.sh` is a FALSE GREEN on macOS — never trust a local run.** Two independent pre-existing reasons: `mapfile` needs bash 4+ (macOS ships 3.2), and **BSD awk silently drops `ORS="\0"`** — `printf 'a;b;c' | awk 'BEGIN{RS=";";ORS="\0"}{print}' | od -c` emits **zero NUL bytes**, so the statement-splitting loop makes zero iterations and the script reports "additive / backward-compatible" for *everything*. CI (Ubuntu, bash 5 + gawk) runs it correctly. **Verify this gate in CI, never on a laptop.** Not fixed in CC-30 — changing a CI gate's splitting mechanism deserves its own packet.
  - **The all-zeros SHA does NOT fail closed** (contrary to the packet's assumption). `git rev-parse --verify --quiet 000…0` **exits 0** — git accepts the null SHA syntactically — so the script's base-resolution guard passes; the subsequent `git diff 000…0...HEAD` errors, but that error is swallowed by the existing `2>/dev/null || true`, leaving an empty file list and a green "no migrations to check". Hence `deploy.yml` skips that case **explicitly with a loud `::warning`** rather than letting the script run. Item **f** (now done) removes the force-push path that reaches it.
  - **No cron route tests exist at all** — `grep -rln 'cron/dispatch\|api/cron' tests/` returns nothing. So CC-30's new `advisory-lock-connect-error` 500 branch has **no automated coverage in any environment**; it is first exercised by a real DB outage. Inherited gap, not created by CC-30.
  - **Two `deploy.yml` paths remain unproven by any real run:** migration-safety has never seen a push that *actually contains* a migration (both CC-30 deploys correctly reported "no added/changed migrations"), and the all-zeros skip branch has never fired. The wiring itself IS proven — first execution on `08aee2d` resolved `GITHUB_BASE_REF: development` from `github.ref_name` and a real `BEFORE_SHA`, so it passed properly rather than vacuously.
- **CC-29 offline trust floor — ✅ MERGED to development/staging (PRs #202/#203/#205), 2026-07-28; on-device smoke is the last gate.** Closes review §1 (1.1–1.6). PR-1 queue engine: `PhotoUploadError.serverReached` (a server-rejected photo burns retries + fails naming the photo, then `continue`s past the wedge), 12s/20s fetch timeouts (lie-fi), online-401 park in `mutate()` (`reason:'auth'`), cross-instance flush mutex (`navigator.locks` + localStorage-lease fallback) + transient-409 body-sniff (`IDEMPOTENCY_IN_FLIGHT_ERROR`), dead-`localphoto:` client enqueue — plus the `tests/offline` flush-lifecycle harness (fake-indexeddb, 8 cases). PR-2 date semantics (D26): client stamps `businessDate()` at submit + read-only Date field; server bounded 3-day past window, past-date collision → 409 (surfaced, never merged), alert gating on `isToday`, daily-check POST now `withIdempotency`-wrapped; server `photoUrlsField()` rejects `localphoto:` → 422. PR-3 (D27): sliding renewal in the proxy past half-life, 14-day cap via `authAt`, in one edge-safe `session-edge.ts`. CI green (incl. node DB suite g,h). **REMAINING (Max, physical phone — not scriptable):** airplane-mode→reconnect per PR; photo-wedge behind a broken upload → later daily check still syncs + photo shows Failed + Discard works; overnight-queue replay files under the performed day; forced-expired online submit shows "check saved — sign in"; session-renewal boundary crossing (no mid-shift sign-out) + force-logout still boots. Browser-observable smoke on staging done by Claude (see handoff).
- **CC-15 Deployment Map — ✅ SHIPPED & smoked (D14).** PRs #197 (GPS capture) + #198 (maps) merged to `development`/staging; Max's staging smoke passed (3 recency pins green/amber/red + route trail + crew map). Synthetic smoke data (`seed-cc15-*`) fully cleaned up — 0 rows remain. `AHITS_MAPBOX_TOKEN` ENABLED + mounted (now mandatory for every staging deploy; **prod cutover (D1) will need `AHITS_PROD_MAPBOX_TOKEN`**). Step 7 (weather stamps) OUT. **Residual:** the iOS A6-Lite re-run on the post-merge build now includes **row 29** (location grant/deny) — see below.
- **Copy-link invites — ✅ SHIPPED (PR #200).** Email-independent onboarding; staging-smoked (incognito setup + regenerate → old link 404s). No residual.
- **CC-26 authenticated click-through — ✅ VERIFIED (Max, 2026-07-20).** The end-to-end alert deep-links were eyeballed on staging by a logged-in admin: failed-check alert → the exact check (answers/odometer/site/photos), missed-check alert → the operator's deployment drawer, and both the vehicle drawer's check rows and the deployment drawer's per-vehicle "View checks" reach the read-only viewer. **CC-26 acceptance is now fully met** (CI + component/DB tests + this manual pass).
- **A6-Lite device pass — Android ✅ GREEN (Max, 2026-07-20, rows 2/5/6/19/23); iOS ☐ STILL REQUIRED before the 07-27 start.** D13 was amended 2026-07-20: the cohort now includes **iOS operators**, so A6-Lite spans both platforms — rows 2/5/6/19/23/**29** must ALSO pass on **one iOS device (installed PWA)** (D14 added row 29). **This is the one outstanding pre-start gate** (charter signed ✓ + Android A6-Lite ✓ + iOS A6-Lite pending). **The full A6 matrix stays PARKED — trigger unchanged: before CC-17 ships** — parked scope is now the **edge rows (20–22) + all-column ceremony**, not the iOS A6-Lite rows (those are pulled forward). **CC-25 coupling:** the iOS A6-Lite includes row 23 (live scan) on the installed PWA, so it covers iOS live-decode; CC-25's *fuller* acceptance (torch, permission-denied fallback, app-switch resume, honest strings, the Safari-tab jsQR path) still rides the pre-CC-17 full run.
- **CC-22 live acceptance pass (Max, on staging) — ⤴ SUPERSEDED by the CC-30 live-fire checklist above** (its (a)/(b)/(c) are CC-30 items **d**/**b**/**c**; track them there, not here). Original text kept for context — the code shipped and smoked, but three checks need real credentials/timing I couldn't exercise from a sandbox: (a) with the real `SENTRY_DSN`, use the `/admin/settings` → Sentry diagnostics buttons to confirm a server error and a client error both capture with `request_id` attached (client event passes CSP); (b) confirm the healthchecks.io ping fires on a real cron run; (c) stop the staging cron >30 min → confirm `CRON_SILENT` appears on the admin dashboard and the next successful run resolves it.
- **CC-12 PR3 My-Deployment Android device-pass (Max)** — the split is green in CI and passed the unauthenticated staging smoke (root→/login 307, /login 200, /operator/my-deployment gates to /login, no 500), and Max gave the "go" to merge after his device pass. **Behavior parity on the real device is the acceptance that matters (D10 / the packet framing): a split that's green in CI but subtly reorders a dialog only shows up there.** Worth a standing re-confirm on the next full device pass: vehicles card (Rental/"Agreement needed" badges, add/remove/select), kit card (item-type + qty chips, low-stock warning, per-row Log-usage vs Return opening the RIGHT item's dialog — the `kitItems`-memo path), transfer/handoff/end buttons, and offline-queue behavior (PR1's OUTBOX + 401-parked surfaces). **Admin/deployments + admin/inventory splits are NOT unfinished CC-12 — they are deferred by decision D10 (demand-pull, owner CC-18).**
- **CC-14 "Today" — device-pass done, known v1 limitation to keep in mind (Max).** Max ran the PR2 Android device pass and gave the "go" to merge the stack (all four PRs merged + deployed green, migrate applied). **Known v1 scope, NOT a bug:** `getActiveRigForOperator` is **PRIMARY-only**, so a **secondary operator sees "No active deployment"** on Today; and a not-deployed operator reaches the daily check only via the bottom-nav Check tab (Today's Start-daily-check action needs a deployment). Both are deliberate v1 calls (recorded here so they don't get re-filed as breakage). **Offline cold-open** now depends on the new `/api/operator/today` SW field-read cache — a first-ever offline load with no cache shows the error/empty state (accepted). The **pilot metric-1 denominator exists** (`/api/admin/pilot-metrics`) — surface it wherever the pilot dashboard lives. The **falsifier ("operators deep-link past Today")** is only instrumented as far as the denominator + route hits, not a full deep-link tracker (noted, not built).
- **CC-23 / CC-24 visual re-check on a real device (Max)** — deploy smokes confirmed the rendered deltas, but several interaction/viewport checks couldn't be driven from the sandbox browser (row-clicks/typing didn't register; the window wouldn't render below desktop; the CC-24-subtraction staging session had expired so the merged card / "Mark Handled" / 2-tap remove weren't eyeballed). All are mechanical/responsive/label changes with passing component tests. Worth a 2-min eyeball on the next device pass: the 5 admin drawers (no clip), daily-check 44px toggles, OfflineBanner collapse-to-one offline, the CC-24-addendum 390px action-row stack, the merged single "Scan / Check Out · In" dashboard card, the per-row ⊖ removing in ≤2 taps via DispositionDialog, optional-note preset chips, and "Mark Handled" on a material request.

## 4. Next actions (ordered)
-2. **UXP-1 nav trust — Max's evening staging smoke of PR-1 #232 + PR-2 #233 → merge both (order-independent) → paste D30/D31/D32 into `DECISIONS.md` (mark D28 `Superseded-by: D30`).** Smoke rows in the handoff (`AHITS_SESSION_HANDOFF_2026-07-29_UXP1.md`) / §3. Then: **next session builds 1e** (back-button history guard, the sole remaining UXP-1 item, P1), then **UXP-2 (Sunlight & Touch)** per Max's packet order (UXP-1 → UXP-2 → UXP-3, `AHITS_UX_PACKETS_2026-07-29.md`).
-1. **2026-07-28 RE-BASELINE (supersedes the items below where they conflict):** the pilot-floor queue is now **~~CC-29~~ ✅ → ~~CC-32~~ ✅ (all four #209/#210/#211/#212 MERGED) → ~~CC-30~~ ✅ → ~~CC-31~~ ✅ (FULLY MERGED #213/#214/#215 + PR-1, preview-smoked) → ~~CC-16S~~ ✅ (#220 TTL-warmup + #221 read-after-leak MERGED) → ~~CC-33~~ ✅ (#223 dead-code/forward-removal + #224 unified-transfer MERGED 2026-07-29, staging deploys green) → ~~CC-34~~ ✅ (PR-1 #226 + PR-2 #227 + **PR-3 #229 schedules** all MERGED 2026-07-29, staging deploy green) (`AHITS_CC29-31_PILOT_FLOOR_PACKETS.md` + riders in `AHITS_PACKET_ERRATA_2026-07-29.md`). **THE HIGHEST-VALUE NEXT ACTIONS:** (1) **Max's CC-34 staging smoke** — the 3a/3c/3d surfaces (create a 90-day Wintex schedule; close a unit repair to an active deployment; "In repair" caption) + run the stale-damage count SELECT read-only vs staging (query in #229's body; the cron nag is bounded 5/run); (2) **Max's owed CC-33 two-phone Transfer smoke** on live staging (RIDER B #7 SELECT already ran → 0 live rows, so no drain was needed); (3) delete the two `ahits-web-app-preview-cc31-*` preview services. In parallel, Max runs the **CC-30 live-fire checklist** (backups/PITR + restore drill, healthchecks, CRON_SILENT, Sentry, branch protection, Mapbox token restriction — none ever attempted) and creates **Stewart's ADMIN account** (D20). Post-floor queue: **secondary-operator Today packet (pre-CC-17, review §3.2)** → ~~D11 glossary sweep~~ (FOLDED INTO CC-32 PR-1) → **CC-17 full-replace-Clockify (D19: measure Clockify's daily taps first, invoicing wedge early, earnings view in DoD, full A6 matrix first)** → CC-18 (customer = Max per interview; re-scope check at commit time). CC-16-proper PARKED (D18). Also carried from the review: onboarding ritual → one-pager (D17), charter gains a "someone gets hurt" line, baseline the group-text volume before first operators go live (the missing control arm, review §6.1).
0. **CC-15 is SHIPPED (D14) — one residual:** the **iOS A6-Lite** pass on the post-CC-15 build now spans rows **2 / 5 / 6 / 19 / 23 / 29** (row 29 = location grant AND deny at the daily check); Android needs a quick re-verify of rows **2 / 19 / 29**. Target Fri 2026-07-24. Prod cutover (D1), when it happens, needs `AHITS_PROD_MAPBOX_TOKEN`. **Copy-link invites (PR #200) is shipped — no residual.**
1. **Pilot fortnight** → CC-15/16/17/18. **CC-27 (the scheduled fortnight filler) is done** (PR #196 — FulfillmentChecklist MUI re-skin, hub-flow smoke passed). **The pre-pilot gates are done:** CC-26 (daily-check viewer, PR #195) was the last must-land-before-pilot packet; CC-14 (Today view, PRs #190–#194) and CC-12 (PRs #187–#189) are done; the reflection-increment sequence (CC-22 → CC-25) is complete. **The charter is SIGNED (2026-07-20) and D5/D6/D7 are resolved** (9 operators, project CUL005, D5=Option A, D7=Stewart Arbuckle). **🟡 PILOT GO PENDING ONE GATE — start Monday 2026-07-27 (contingent).** Charter signed + A6-Lite **Android** green (both 2026-07-20). **Outstanding pre-start gate: the A6-Lite iOS pass** (installed PWA, rows 2/5/6/19/23/29) — D13 amended 2026-07-20 to include iOS operators. Full A6 matrix parked to pre-CC-17 (edge rows 20–22 + all-column ceremony). **Remaining before start: the iOS A6-Lite pass (§3) — now the sole outstanding item** (Max's escalation phone is filled). **The D9 glossary sweep is re-deferred as D11** (owner: next session, strings-only, precondition met). Of the 5 pending primitives, **`EmptyState` shipped demand-pull in CC-14**; the other 4 (MobileCardTable, EntityCard, PageHeader, FilterBar) remain demand-pull, **NEVER a standalone sweep**.
2. **Launch prep for Monday 2026-07-27:** **(a) run the A6-Lite iOS pass** (installed PWA, rows 2/5/6/19/23/29) — the sole outstanding pre-start gate; **(b)** the **`EMAIL_SANDBOX` flip is decoupled from Day 1 (D15)** — flip it whenever the Resend sending domain is DNS-verified AND both D6 audits pass (pilot-hub addresses + non-hub recipient paths), before or after Monday; email is optional at launch (LINK invites onboard by text); **(c)** print the triage card (escalation phone now filled). **The full A6 matrix (edge rows 20–22 + all-column ceremony + CC-25's fuller live-camera acceptance) stays parked — trigger = before CC-17 ships.**
3. **D5/D6/D7 are RESOLVED (2026-07-20)** — D5=Option A (CC-28 permanently moot), D6=rule confirmed (flip on start date, not before), D7=Stewart Arbuckle. See `DECISIONS.md`. (D8 was resolved earlier, folded into D3.)

## 5. Open decisions (undecided — need a human)
- ~~**D5 / D6 / D7**~~ — **all RESOLVED 2026-07-20** (charter signed): D5=Option A (CC-28 moot); D6=sandbox-flip rule confirmed, executed on the start date after the two audits; D7=Stewart Arbuckle (+44 7747 738364). See `DECISIONS.md`.
- Governance sweep items (push vs 45s polling; CARRY-* build-or-descope) — see workplan §13.
- Mounted collection units: reuse-vs-new-model design decision (CC-21 spike) before building.

## 6. Do-not-touch / deferred (pointers only — these are NOT gates)
- **Production cutover** → DEFERRED, see `DECISIONS.md` D1 + `AHITS_PROD_CUTOVER_DEFERRED.md`.
- **W0-10 `4b′`/`4c` patches** → HELD, see `DECISIONS.md` D4 (they live in `held/`).
- **Real-time GPS tracking** → permanent anti-goal, see `DECISIONS.md` D2 (crew visibility is last-known only).

## 7. Map to the deep docs
- **Start here / corpus map:** `00_START_HERE.md`
- **Plan of record:** `AHITS_PHASE3_WORKPLAN_2026-07-10.md` · **Paste-ready packets:** `AHITS_CLAUDE_CODE_INSTRUCTIONS.md` (canonical, supersedes the dated `_2026-07-10`/`_2026-07-12` packet docs) · **Landing order + smoke:** `AHITS_LANDING_ORDER_AND_SMOKE_CHECKLIST.md`
- **Deep assessment:** `AHITS_STATE_OF_THE_APP_2026-07-10.md` · **Strategy:** `AHITS_PHASE3PLUS_NORTH_STAR_v3.md` · **Sequence:** `AHITS_MASTER_ROADMAP.md` · **One-pager:** `AHITS_ROADMAP_EXEC_SUMMARY.md`
- **Decisions:** `DECISIONS.md` · **Latest handoff:** `AHITS_SESSION_HANDOFF_2026-07-29_UXP1.md` · **Rules for changing code:** `CLAUDE.md`

---

## SESSION CLOSE — do all 7 (≈6 min). This is the durability contract.
> **§1 ≡ §2 invariant:** the merged set named in §1 (one-paragraph state) MUST match the merged set listed in §2 (environments). If they disagree, §2's code-grounded list wins — reconcile §1 to it before closing.
> **Parallel sessions:** when sessions run in parallel, the **first-started session owns `STATUS.md`/`DECISIONS.md`**; every other session closes with a **uniquely-named handoff only** (e.g. `AHITS_SESSION_HANDOFF_<date>_<PACKET>.md`) and never edits the shared docs. *(Learned 2026-07-28: CC-29 and CC-30 ran concurrently against one working tree — CC-29 held uncommitted `STATUS.md`/`DECISIONS.md` edits and moved the tree across four branches while CC-30 worked. CC-30 built in isolated `git worktree`s and closed handoff-only; nothing was clobbered. The owning session folds the other's §3 rows in afterwards, as happened here.)*
1. **`STATUS.md`** — update §1 state, §3 active work, §4 next actions, and the date line.
2. **`DECISIONS.md`** — append any decision made this session (new `Dn`); mark superseded ones.
3. **`00_START_HERE.md` / `docs/INDEX.md`** — if you added/superseded/moved a doc, fix the row.
4. **Handoff** — write `AHITS_SESSION_HANDOFF_<date>.md`: what shipped + resume points; reference decisions by `Dn`, don't re-narrate them.
5. **Commit + push** all docs to git. (`git add -A && git commit && git push`.)
6. **Sanity:** `git status` clean, and confirm the docs you wrote appear in `git ls-files '*.md'`. **If a doc you wrote isn't tracked, it does not exist for the next session.**
7. **Verify every merge you claimed (60 seconds).** For each packet you marked merged in §1/§2, grep the code for ONE acceptance string that only exists if that packet landed. Canonical anchors for the new packets: CC-22 → `CRON_SILENT` · CC-23 → the tokens.ts import in the theme · CC-24 → `Mark handled` · CC-25 → `QrScannerDialog` · CC-26 → the daily-check viewer route path · CC-27 → an MUI import in FulfillmentChecklist · **CC-29 → `serverReached` + `ahits-outbox-flush` + `MUTATE_TIMEOUT_MS` + `reason: 'auth'` (useOfflineQueue.ts), `PAST_WINDOW_DAYS` + `already exists for this vehicle` (daily-check/route.ts), `authAt` + `MAX_SESSION_LIFETIME` (**`src/lib/auth/session-edge.ts`** — path corrected 2026-07-28; the anchor previously said `src/lib/session-edge.ts`, which does not exist, so a step-7 run would have wrongly flagged CC-29 as unverified), `photoUrlsField` (validation.ts), and `tests/offline/` present.** · **CC-32 → PR-1 (merged): `grep -rn "Launch Deployment" src` → **0 hits**, `grep -rn "Check In · Out" src` → **0 hits**, `grep -rn "Staged at" src` → ≥1. PR-2 (#210, verify only AFTER it merges): `lastCheckSite` ≥2 files, `Note (optional)` in the my-deployment handoff dialog, `Held until`, `All caught up — everything sent.`, `grep -rn "Pull to refresh" src` → 0, `// CC-32 (2.5)` and `// CC-32 (2.6)`. PR-3 (#211, likewise): `pathname === '/api/map/crew'` in `src/app/sw.ts`, `label: 'Map'` in `OperatorBottomNav.tsx`, `// CC-32 (3.2)` ×≥4. **Do NOT tick these until the PRs actually merge — they are open as of 2026-07-28.**** · **CC-30 → `REFUSING: pr-staging-deploy` (pr-staging-deploy.yml), `migration-safety` + `env-drift` (deploy.yml), `AHITS_DANGEROUS_TARGET` (prisma/seed.ts + Makefile), `advisory-lock-connect-error` (cron/dispatch/route.ts), `PILOT_ROLLBACK.md` tracked.** · **CC-31 → PR-1 (on `development`): `INTERVAL '72 hours'` (cron/dispatch/route.ts), `resolveActiveAlert('PIN_LOCKED'` (users/[id]/route.ts), `resolveActiveAlert('MATERIAL_REQUEST'` (deployment-requests.ts), `resolveActiveAlert('EQUIPMENT_NOT_RETURNED'` (deployments/[id]/end/route.ts), `resolveActiveAlert('INVENTORY_DRIFT'` (cron/dispatch/route.ts). PR-2 (#215, verify AFTER merge): `supersedes link expiry` (status-links receive route), `Expired link` (admin/hubs/page.tsx), `forOperatorId" = ` in `listRequests` (deployment-requests.ts), `requestedById === user?.userId` (operator/requests/page.tsx), `deployment_requests_requestedById_idx` (the cc31 migration). PR-3 (#214, likewise): `admin/pilot` (AdminNav.tsx), `gpsGrantRate` (pilot-metrics.ts). **Do NOT tick #215/#214 until they merge — open as of 2026-07-28.**** If the string is absent, the claim is wrong — CORRECT §1/§2 before closing, do not close on an unverified claim. Velocity without this check is how STATUS went self-inconsistent within a day of its own contract.
